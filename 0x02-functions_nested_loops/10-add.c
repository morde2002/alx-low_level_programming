#include "main.h"

/**
 * add -  function adds two integers
 * @a: the first parameter of the add function
 * @b: the second parameter of the add function
 * Return: 0 for success
 */

int add(int a, int b)
{

	int sum;

	sum = a + b;

	return (sum);

}

